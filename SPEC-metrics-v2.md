# 指标与信息架构第二轮：机群总览、引擎中立的指标模型、SQL 洞察

## Problem Statement

dbs-monitor 现在的信息架构建立在两个已经不成立的前提上。

**前提一：使用者只管几台实例。** 登录后落地页就是实例列表，没有任何机群层面的汇总。一台出事和三百台出事，界面呈现完全一样——都是一张按健康度排序的表，要靠人往下滚。实际的机群规模是 500 台以内，这个数量下「打开就知道整体怎么样」必须由一个专门的页面回答，而不是由一张表的前几行暗示。

而且列表本身撑不住这个规模：`GET /api/v1/instances` 返回裸数组，没有分页、没有服务端筛选，筛选和排序全在浏览器里做；列表每一行还各自发一次 `/metrics/series` 请求画趋势图,500 行就是 500 个并发请求。

**前提二：只监控 PostgreSQL。** MySQL 已经在近期路线图上。当前代码里没有任何多引擎的痕迹：41 个指标 ID 全是 `pg.` 前缀且写死在 `metric_series` 的 CHECK 约束里，术语表里的凭据叫 **PG credential**，`Instance.database` 是必填字段——而 MySQL 监控的是服务器实例，没有「连到哪个库」这一说。

除此之外还有两个独立的缺口：

**指标目录缺了 DBA 最先看的几个数。** 现有 33 个 `pg.*` 指标覆盖连接、事务、复制、锁、会话，但**没有缓存命中率、没有数据库体积、没有死锁数**——成熟的数据库监控产品（Percona PMM、阿里云 DAS、Datadog DBM）首屏几乎都有这三个。另外连接数是个裸数字：「连接数 380」在不知道 `max_connections` 是 400 还是 4000 时没有意义，而 500 台机群里没人记得住每台的上限。

**慢 SQL 没有全局出口，而且看不出是哪条语句。** 查询统计埋在实例工作台的第三层页签里，DBA 的实际动线却是「哪条 SQL 在拖垮我的机群」。更要命的是 `query_statistics_snapshot_entry` 和 `long_query_sample` **都不存 SQL 文本**——页面上只能显示 `queryid 8231447290183` 和 `database_oid 16384`，没有人认得出那是什么。

## Solution

四件事，一份规范。

1. **新增机群总览页**，作为登录落地页，回答「整体还好吗 / 我现在该看谁 / 谁在拖后腿」。
2. **把指标模型改造成引擎中立**：立一组语义位，给时序表加库维度，把指标目录从 CHECK 约束搬进表。本轮**不写一行 MySQL 采集代码**——只保证第二个引擎接入时不需要改界面。
3. **重新定义实例的粒度**：一个实例是一条数据库连接、一个服务端点，可以包含多个库。库降级为指标的一个维度。
4. **把慢 SQL 提到一级模块**，并让它真的显示 SQL。

对使用者而言，结果是：

- 登录后先看到机群的健康分布、采集自身的健康、以及需要优先处理的实例，每个数字都能点进筛好的列表。
- 实例列表在 500 台下仍然可用：服务端分页与筛选，一次请求拿回所有趋势。
- 列表上能一眼看到连接饱和度（百分比），而不是一个不知道分母的连接数。
- 一个「SQL 洞察」模块，跨实例按总耗时排序，显示归一化后的 SQL 文本。
- 在实例工作台里能按库看体积、命中率、死锁——一个连接下建了几十个库时，能指出是哪个库在吃磁盘。

## User Stories

### 机群视角

1. 作为值班 DBA，我希望登录后第一屏就看到机群按健康档位的实例计数，这样我不用滚动就知道今天是不是平静的一天。
2. 作为值班 DBA，我希望这些计数每一个都能点，点完落到已经筛好的实例列表，这样我不必再手工设一遍筛选条件。
3. 作为值班 DBA，我希望首屏有一块专门显示采集自身的健康（数据不新鲜的实例数、Agent 离线数、采集暂停数），因为 500 台里最容易悄悄烂掉的就是这一层。
4. 作为值班 DBA，我希望首屏列出最需要关注的十台实例，而不是全部五百台——五百个格子没有信息量。
5. 作为 DBA，我希望首屏有一块磁盘水位最高的十台，因为这类指标不会报警但会要命。
6. 作为 DBA，我希望首屏能看到最拖后腿的五条 SQL，点进去是完整的 SQL 洞察页。

### 实例列表在 500 台下

7. 作为 DBA，我希望实例列表按页加载，筛选和排序由服务端完成，这样五百台时页面不会卡住。
8. 作为 DBA，我希望筛选条件体现在地址栏里，这样我能把「当前所有严重实例」这个视图发给同事。
9. 作为 DBA，我希望列表上直接显示连接饱和度的百分比，这样我不用记住每台的 `max_connections`。
10. 作为 DBA，我希望列表上能看到实例的引擎类型，这样 PG 和 MySQL 混在一起时我知道自己在看什么。
11. 作为 DBA，我希望列表上的每一列都能帮我判断「要不要点进这一行」，帮不上的信息应该下沉到详情页而不是占着宽度。

### 多库与多引擎

12. 作为 DBA，我希望一个数据库连接下的所有库算**一个**实例，因为我在一个连接里可能建几十个库，一库一实例会让实例数膨胀到无法管理。
13. 作为 DBA，我希望在实例工作台里能按库展开体积、缓存命中率、死锁数，这样我能指出是哪个库出的问题。
14. 作为 DBA，我希望列表和总览显示的是实例级的汇总值，不要按库展开——否则行数会以另一种方式膨胀。
15. 作为 DBA，我希望一条「缓存命中率过低」的告警规则在 PostgreSQL 和 MySQL 上都能用，不用为两种引擎各写一遍。
16. 作为平台管理员，我希望接入 MySQL 时不需要填「数据库名」，因为那个概念在 MySQL 上不存在。

### SQL 洞察

17. 作为 DBA，我希望有一个跨实例的 Top SQL 页面，按总耗时排序，这样我能直接找到最费资源的语句。
18. 作为 DBA，我希望这个页面显示的是 SQL 文本而不是 queryid，否则我无法把它对应到应用里的哪一段代码。
19. 作为安全负责人，我希望平台不要把带真实参数的 SQL 原文存进数据库，因为那些参数里可能有身份证号和手机号。

### 指标覆盖

20. 作为 DBA，我希望能看到缓存命中率，这是我判断一个库健不健康的头一个反射动作。
21. 作为 DBA，我希望能看到数据库体积和它的增长率，这样我能提前几周发现磁盘要满。
22. 作为 DBA，我希望能看到死锁数，因为死锁是应用层并发问题最直接的信号。
23. 作为 DBA，我希望一个 200GB 的主库命中率崩了不会被同实例下二十个空库的 100% 稀释掉。

## Implementation Decisions

### 实例的粒度

一个 **Monitored instance** 是一个数据库服务端点、一条连接，可以包含多个库。**Database** 降级为指标的一个维度值，不是被监控实体：它没有自己的健康状态、告警和采集配置。

`Instance.database` 改称 **Bootstrap database**——建立连接用的库名，不限定监控范围。PostgreSQL 必填、默认 `postgres`；MySQL 留空（字段可空）。

### 引擎中立：语义位，不是全中立命名

立 9 个**语义位**。每个位是一层指向，按引擎解析到具体的指标 ID；现有 `pg.*` 一个都不改名。

| 语义位 | 级别 | PostgreSQL 绑定 | 状态 |
|---|---|---|---|
| 吞吐 | 库级 | `pg.tps` | 已有 |
| 连接数 | 实例级 | `pg.connection.total` | 已有 |
| 连接饱和度 | 实例级 | `pg.connection.total / max_connections` | **新增** |
| 探针延迟 | 实例级 | `pg.probe.latency_ms` | 已有 |
| 回滚率 | 库级 | `pg.xact.rollback_per_sec` | 已有 |
| 复制延迟 | 实例级 | `pg.replication.replay_lag_ms` | 已有 |
| 缓存命中率 | 库级 | `pg.cache.hit_ratio` | **新增** |
| 容量水位 | 库级 + 主机 | `pg.database.size_bytes`、`host.disk.usage_percent` | **新增** |
| 死锁数 | 库级 | `pg.deadlock.count` | **新增** |

总览页、实例列表、告警规则模板**只能引用语义位**；实例工作台可以下到具体指标 ID。

WAL 保留、复制槽、prepared xacts、autovacuum、临时文件**不立位**——MySQL 没有对应物，强行立位只会得到名不副实的 ID。它们继续留在 PostgreSQL 工作台里。

理由记在 `docs/adr/0001-semantic-metric-slots.md`。

### 时序表的库维度

`metric_series` 增列 `database_name text NOT NULL DEFAULT ''`，唯一键变成 `(instance_id, metric_id, database_name)`。空串表示实例级指标。`metric_sample` 不变。

理由记在 `docs/adr/0002-single-named-metric-dimension.md`。

### 指标目录进表

新建 `metric_catalog` 表，`metric_series.metric_id` 外键引用它，删除 `metric_series` 上写死的 `metric_id` CHECK 枚举。目录列：指标 ID、引擎、单位、展示名、语义位归属、级别（实例级 / 库级）、聚合方式。

前端 `web/src/routes/instances.$id/metricOptions.ts` 里的展示名与单位下沉到这张表——跨引擎之后前端不该再持有指标目录。

项目仍在开发阶段、没有任何部署，所以**直接改写 `migrations/00001_walking_skeleton.sql` 里的表定义，不叠加新迁移**。迁移文件的历史在没有部署的时候没有价值，叠加只会让 `metric_series` 的最终形状散在两个文件里。

### 库级指标怎么聚合成实例级

列表和总览一律显示实例级值。库级指标的聚合方式**逐指标定义**在 `metric_catalog` 里：

- **比率型取加权平均**：缓存命中率按 `blks_hit + blks_read` 加权。不用算术平均——一个 200GB 主库崩到 60% 会被二十个空库的 100% 稀释成 98%。
- **计数与体积求和**：死锁数、TPS、回滚数、数据库体积。

注意 `pg.tps` 的语义因此改变：现在它是「那一个库的 TPS」，之后是「该实例下所有库之和」。

### 新增的四项采集

全部可以从**单条连接**拿到——已核实 `pg_stat_database`、`pg_database_size()`、`pg_stat_statements`、`max_connections` 都是集群级视图，连到哪个库都读得到。这是「一个连接管一台服务器」在采集侧成立的前提。

- `max_connections`：配置项，低频采集即可。
- `pg.cache.hit_ratio`：`pg_stat_database` 的 `blks_hit / (blks_hit + blks_read)`，按库。
- `pg.database.size_bytes`：`pg_database_size()`，按库。增长率由查询侧对时序求差得出，不单独采。
- `pg.deadlock.count`：`pg_stat_database.deadlocks`，按库，累积计数转速率。

对象级的指标（表/索引体积、膨胀、索引使用率）**不在本轮**——它们是唯一需要逐库建连接的一类。

### 告警

告警规则模板增加引擎归属：引用语义位的模板一份两用，引用引擎私有指标的模板只对该引擎可见。规则作用域选实例时按引擎过滤。

**告警判定只在实例级聚合值上进行**，本轮不做按库告警。按库告警会把作用域从「实例集合」变成「实例集合 × 库」，波及评估状态机、告警归因、维护窗口和通知去重——那是一份独立规范的体量。加权平均这条聚合规则先把稀释问题挡住。

### 信息架构

侧栏从 4 项变成 6 项、分三组（`SideNavDivider` 已有）：

- **监控**：总览 · 实例列表 · SQL 洞察
- **告警**：全局告警 · 告警设置
- **系统**：用户管理

登录落地页从 `/instances` 改为 `/`。

### 总览页

五块，从上到下——对应 DBA 打开页面时依次问的三个问题：

1. **机群健康计数**：严重 / 警告 / 未知 / 正常 / 暂停。每个数字可点。
2. **采集自监控**：数据不新鲜数、Agent 离线数、采集暂停数。
3. **需要关注的实例 Top 10**：按健康档位 + 告警严重度排序。
4. **容量水位 Top 10**：磁盘使用率最高的十台。
5. **Top SQL 前 5**：跨实例按总耗时，点进 SQL 洞察全页。

多引擎落地后，1 和 2 按 Engine 再分一层。

**不做**：全量实例格子墙（500 个格子没有信息量）、装饰性 KPI 环形图（`DESIGN.md` 的 Data visualisation 一节禁止）。

### 实例列表

八列，974px 预算内：

**实例**(grow) · **引擎**(56) · **健康**(90) · **告警**(90) · **告警归因**(grow) · **连接饱和度**(96) · **吞吐趋势**(72) · **采集新鲜度**(127)

相对现状的取舍：

- **地址**整列去掉。500 台时没人靠 IP 认机器，靠名字；地址进详情页和搜索索引。
- **标记**并入健康列（暂停 / 维护 / 无数据作为健康圆点的后缀，复用 `SuppressionTags`）。
- **Agent** 并入采集新鲜度——「Agent 离线」本来就是新鲜度失效的一种，两列在说同一件事。
- **趋势**从连接数改成吞吐：连接数已经由饱和度列用数字表达，趋势再画一遍是重复。

判据：每一列必须回答「我该不该点进这一行」，回答不了的下沉到详情页。

### 列表 API

`GET /api/v1/instances?page&page_size&q&engine&status&flags&severity&sort` → `{ items, total }`。

这是对现有端点的**破坏性变更**（现在返回裸数组），直接改，不做兼容层、不加版本号：自用 API、前后端同仓库、同一个二进制（`go:embed`），没有第三方消费者。可选分页参数会带来两条码路各自维护，保护的却是一个不存在的调用方。

每行单独请求趋势改为**一次批量端点**：500 个并发 series 请求不只是慢，会打满后端连接池。

总览页每个可点数字的下钻链接，直接落成这套查询参数——这是「下钻动线依赖服务端筛选」的具体含义，也是列表服务端化必须与总览页同批交付的原因。

### SQL 洞察与 SQL 文本

**只落 `pg_stat_statements` 的归一化文本**（字面量已经是 `$1`，这是该扩展的设计保证）。`pg_stat_activity.query` 是带真实字面量的原始语句，可能含身份证号、手机号、口令——**不落库**。长查询采样继续只显示耗时、等待事件和阻塞关系。

文本**按 `(instance_id, queryid)` 去重存一张单表**，快照表只存指标。500 台 × 每台 Top 500 条 × 归一化文本均值 300 字节 ≈ 75MB，如果每次快照都存全文会迅速失控。

MySQL 侧 `events_statements_summary_by_digest.digest_text` 是同性质的归一化文本，这个选择天然跨引擎对齐。

## Testing Decisions

### 什么是一个好的测试

只断言外部可观察的行为，不断言实现细节。一个测试如果因为内部重构而变红，它测的是结构不是行为。

### 缝的选择

四条缝，全部是现成的，**不新建缝**。能放在更高的缝上断言的，就不往下放。

1. **`internal/httpapi` 集成测试**（主缝，走真实 HTTP + 真实库）。绝大部分行为在这里断言：
   - 实例列表的分页边界、筛选组合、排序稳定性——现有 `handler_integration_test.go` 已有实例列表的集成测试，扩展它。
   - 批量趋势端点：一次请求覆盖多个实例。
   - 总览各块的聚合结果。
   - SQL 洞察端点，含**文本去重的可观察行为**：同一条 SQL 采两次，接口只返回一条、且文本按最新的更新。这一条刻意放在 HTTP 层而不是仓储层——测的是接口的表现，而不是表里有几行。
   - `engine` 字段与可空的 bootstrap database。

2. **`internal/metric` 纯函数**（现有 `dictionary_test.go`、`rate_test.go` 是先例）。只放两样东西：
   - **库级 → 实例级的聚合规则**。必须有「一个 200GB 主库崩到 60% + 二十个空库 100%」这个用例——它是加权平均存在的唯一理由，埋进集成测试的 fixture 堆里就没人看得出它在测什么。
   - **语义位解析**（语义位 + 引擎 → 指标 ID），覆盖「该引擎没有这个位」。

3. **`web/` 投影函数**（沿用 `instanceProjection.ts` 的模式，`overview.test.ts` 等是先例）。总览各块的数据投影、列表列的呈现。页面组件只测「数字可点且链接带对了查询参数」。

4. **`web/e2e/`**。两条新动线：登录落地到总览、从总览的健康计数下钻到筛好的实例列表。

### 什么不测

不为总览页做像素基线。视觉验收沿用 `scripts/qa-shots.mjs` 的人工逐页比对，新增总览页与 SQL 洞察两条路由。

### 门禁

`make check` 全绿，e2e 覆盖上述两条新动线。

## Out of Scope

- **MySQL 采集实现**——本轮只保证模型和界面能容纳它。
- **值班墙大屏**（NOC wall）。那是另一套排版规则：1920 起、几米外看、无交互。让它污染主界面的栅格是错的。
- **全中立指标命名**与随之而来的历史数据迁移。
- **通用标签模型**（Prometheus 式 jsonb labels）。
- **按库告警**。
- **对象级监控**：表与索引体积、膨胀、索引使用率。这是唯一需要逐库建连接的一类。
- **`pg_stat_activity` 原文落库**。
- **存量数据兼容**。项目仍在开发阶段，没有任何部署，不需要迁移路径。
- **把 `host.*` 指标改挂到主机而不是实例上**。引入这个概念原本是为了解决「一实例一库导致主机指标重复采集」，而实例重新定义成「一条连接」之后那个理由基本消失了；剩下的只是「一台机器上跑两个数据库服务」这个次要情形，不值得新建主机实体、改 Agent 上报归属并迁移挂载点。

后续 issue：按库告警 · 对象级监控 · MySQL 采集 · 值班墙 · 主机指标归属。

## Further Notes

### 与 #210 的关系

`feat/carbon-redesign`（PR #210）的口径是「只换视图层、不改功能」。本轮是纯功能变更，因此走独立分支与独立 PR。侧栏从 4 项变 6 项、落地页改到 `/`，都要等 #210 合并之后再动，否则那个 PR 会再也审不动。

### 交付顺序

列表服务端化必须先于总览页可用——总览的核心动线是「点数字下钻到筛好的列表」，先做总览后补分页，等于总览上线即带一条走不通的主路径。拆成两个 issue，但同一份规范。

### 已知的成本，尚未实测

- `pg_database_size()` 在库很多时的开销没有量过。若成为问题，退路是降低该指标的采集频率（体积是慢变量），而不是放弃它。
- 归一化 SQL 文本表的去重写入在高频快照下的写放大没有量过。
