# R2 决策索引 v1.0

> 本文把 Wayfinder 地图 [#15 · R2 系统架构骨架与技术选型](https://github.com/liumingjian/dbs-monitor/issues/15) 的 `Decisions so far` 固化到仓库。后续实现会话读本文和对应设计文档，不依赖 issue 正文仍保持可见。
> 状态：R2 决策层已收口；T11 walking skeleton 已验收。实现路线进入 R2 收口与 `/to-spec` 前置。

## 1. 贯穿路线的边界

- PG-only 私有化部署；服务端与 Agent 用 Go，前端是 TypeScript + React + Vite 纯 SPA，静态资源由 `go:embed` 打入主二进制。
- 交付采用离线 tar，自带 PostgreSQL；平台端与 Agent 支持 linux/amd64、linux/arm64，PG 指标由服务端直连采集，Agent 只采 OS 指标与心跳。
- 不引入 Prometheus 全家桶、第二存储引擎、容器交付、实例级授权或 R1 已否决的 `Silence`、`SuppressionPolicy`、`SUPPRESSED` 状态。
- R1 四条不变式继续有效：告警五状态与压制正交、实例健康单一来源、三档角色且凭据永不回显、三条内置采集状态规则不可删不可停用且严重级别不低于 `warning`。

## 2. 已冻结决策

| 票 | 仓库文档 / 研究 | 决策 gist |
|---|---|---|
| T1 | [issue #19](https://github.com/liumingjian/dbs-monitor/issues/19) | 单 Go 服务端承载服务职责；PG 指标一律服务端直连，Agent 只采 OS + 心跳；能力探测和告警评估独立循环，Agent 与实例 1:1，凭据只归服务端。 |
| T3 | [issue #21](https://github.com/liumingjian/dbs-monitor/issues/21) | Agent 使用同一份 OpenAPI 的单上报端点；强制 TLS、自签 CA、令牌只写边界；心跳与离线共用时间门槛，时间偏移超限拒收。 |
| RT-C | [issue #16](https://github.com/liumingjian/dbs-monitor/issues/16) | 保持自带 PG + 原生分区；容量与查询门槛须用真实 PG 实测，未以推算冒充证据。 |
| RT-D | [issue #17](https://github.com/liumingjian/dbs-monitor/issues/17) | `net/http` / 标准库为底，oapi-codegen strict server，sqlc + pgx 逃生舱，goose 自动迁移，Agent 使用 gopsutil。 |
| RT-E | [issue #18](https://github.com/liumingjian/dbs-monitor/issues/18) | 选 AntD 6、ECharts 6、自写图表 wrapper、TanStack Query 与 TanStack Router；缺数保留为 `null` / 缺桶，不补 0。 |
| T2 | [04-metric-storage-model.md](04-metric-storage-model.md) | 自带 PG 原生分区，窄表样本模型；非数值可画曲线的状态编码为 float8，派生采集状态留在控制面；差分写入侧完成，最新查询带时间下界。 |
| T5 | [05-backend-code-structure.md](05-backend-code-structure.md) | 按领域垂直切包，L3 cmd → L2 编排 → L1 领域 → L0 基础设施；同层默认禁止，新包与 interface 必须显式登记，领域包只接 `DBTX`。 |
| T4 | [06-metric-dictionary-and-collection-plan.md](06-metric-dictionary-and-collection-plan.md) | 字典口径编译进 Go，计划参数入库；采集一等公民是 Task；能力是封闭四态；PG13–17 矩阵与任务唯一产出测试属于 R3。 |
| T6 | [07-api-contract-and-codegen.md](07-api-contract-and-codegen.md) | OpenAPI 按域拆分、`make gen` 唯一入口、生成物入库并做漂移门；时间绝对化；空状态 200 + 封闭 13 码；角色声明覆盖每个 `operationId`。 |
| T7 | [08-frontend-stack-and-ui.md](08-frontend-stack-and-ui.md) | 三个状态桶，`domain/` 封闭清单；图表 `unavailability` 必填；枚举用 `assertNever`；轮询渲染用 `dataUpdatedAt`；TimeRangePicker 的骨架内联偏离见该文收口登记。 |
| T8 ⚠ | [09-packaging-and-deployment.md](09-packaging-and-deployment.md) | 离线 tar + 自建 PG17、socket-only、双架构与各自 glibc 下限；升级备控制面、回滚靠备份；时钟和磁盘容量是安装期硬门；整机宕机交外部基础设施。 |
| T9 | [10-ai-guardrails-and-verification.md](10-ai-guardrails-and-verification.md) | `make check` / `make check-full` 两层闭环，现行快层预算为 **≤120 秒**；Docker 真 PG；A/B 护栏登记表、两份 `CLAUDE.md`、CI PR 门；RT-C 由人工 / R3 发布门接管。 |
| T10 | [11-walking-skeleton-slice.md](11-walking-skeleton-slice.md) | 骨架只验证必须跑起来才能证伪的选型：两条采集通路、告警、认证、前端两级路由、分区和真实验收；不预摆 R3–R6 空壳。 |
| T12 | [12-collection-concurrency-timeouts-and-backpressure.md](12-collection-concurrency-timeouts-and-backpressure.md) | 中央调度、探针/查询双槽、分层超时与背压；不补跑、不自动降频；采集源完整性水位；平台自身诊断边界移交 T14。 |
| T13 ⚠ | [13-credential-encryption-rotation-and-revocation.md](13-credential-encryption-rotation-and-revocation.md) | PG password 使用版本化 keyring + AES-256-GCM；Agent 显式登记、token 只存哈希；解密故障属于平台自身故障；秘密永不出站。 |
| T14 ⚠ | [14-platform-observability-and-diagnostics.md](14-platform-observability-and-diagnostics.md) | 平台健康独立四态，journal 是历史，诊断 API 是管理员入口；平台故障不进入目标告警或 `NO_DATA`；磁盘紧急时拒写新样本但不自动删旧数据或缩短保留。 |
| T15 ⚠ | [15-ci-and-release-pipeline.md](15-ci-and-release-pipeline.md) | GitHub Actions 是规范执行者；PR 门为 `make check`，默认分支为 `make check-full`；语义化 tag + 精确提交校验 + Environment 审批；四种原生架构/glibc 组合与长期 Release assets。 |

## 3. 收口注记

- P0 已由 PR #34 合入 `9127f90`；本索引以 P0 回写后的文档为准，不复述地图正文中已过时的 T9 `≤90 秒`。
- P1 首轮只落 PR 门与默认分支 / 手动 `check-full` workflow；四组合原生 runner、发布审批与 Release 归档仍交下游。
- `docs/design/01-appendix-implemented.md` 尚未生成，已在 T4 文档登记为 R3 未兑现项。

## 4. 被后续决策取代的部分

> 本节只登记指针，不改写 §2 的原结论。带 ⚠ 的票表示其决策**部分被取代**，读到该行须一并读本节所指的记录。

| 取代记录 | 取代了什么 | 决策票 |
|---|---|---|
| [18-v1-delivery-boundary-bs-binary.md](18-v1-delivery-boundary-bs-binary.md) · v1 交付边界：B/S 二进制直接运行验收 | **T8** 中依附「离线 tar 安装包」「自带并自建 PG17」「安装脚本」的全部结论（D1、D2、D4、D5、D8、D9.1、D10/D11 的执行者、§3 的 glibc 下限与 §12 交付物清单）；**T15** 中依附「四组合架构 × glibc + 长期 Release assets」的部分（D3.3、D4、D5）；地图 [#98](https://github.com/liumingjian/dbs-monitor/issues/98) 及决策票 #99–#102 的整条 macOS `.pkg` 首发路线。**T2 / T12 / T13 机制 / T14 不受影响**，见该文 §13 点名清单。 | [#106](https://github.com/liumingjian/dbs-monitor/issues/106)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [19-agent-distribution-and-upgrade.md](19-agent-distribution-and-upgrade.md) · Agent 分发与升级形态（无安装器） | **T8** D7 全条与 §8.1（安装脚本自举形态下的自分发、CA 指纹内嵌、plan B 依托 tar 包）、以及 T8 D11 时钟检查的执行者；载体全部改写为「接入设置页生成的安装命令 + 下载端点 + Agent 启动自检」。**「绝不自升级」「装要 root 跑不要 root」「信任根带外传递、全程无 `-k`」「编译期同源 / 运行期容一个大版本」四条原则保留。** | [#108](https://github.com/liumingjian/dbs-monitor/issues/108)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [25-master-key-provenance-and-startup-failure.md](25-master-key-provenance-and-startup-failure.md) · 主密钥来源与启动失败语义（无安装器形态） | **T13** D2 全条（主密钥来源、首启自举、缺失/损坏的进程行为）、D1 的保护面/不保护面清单、D7.1 步骤 1 的「或取得排他维护锁」。改定为：密钥文件是唯一规范来源（环境变量只覆盖路径、KMS 出局）、三条件 + `O_EXCL` 自动生成并显式记事件、**keyring 故障不拒绝启动**（唯一拒启动情形是配置文件读不到）、轮换为停机子命令 + 平台库 advisory lock 拒绝并发。**威胁模型保护面升级**（外部 PG 主机整机失陷不泄露目标库密码），**不保护面新增「配置文件泄露 = 平台库凭据泄露」**（平台库凭据明文留在 `0600` 配置文件，鸡生蛋不可避）。并接下 [18](18-v1-delivery-boundary-bs-binary.md) D5 移交的本地通知快照密钥来源（无变化）。**T13 D3–D6、D8、D9 不受影响**，见该文 §12。 | [#109](https://github.com/liumingjian/dbs-monitor/issues/109)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [26-data-and-recovery-gate.md](26-data-and-recovery-gate.md) · 数据与恢复门的具体证据 | **[25](25-master-key-provenance-and-startup-failure.md) D4「唯一拒绝启动的情形是配置文件读不到」**——拒启动情形扩为四类：配置文件读不到、迁移**执行**失败、迁移锁等待超时、进程锁被占；并**澄清适用面**（不推翻）[18](18-v1-delivery-boundary-bs-binary.md) §8 D7（「迁移失败即拒绝启动」只管执行失败，连不上库不算）、§6 D5 第 1 条（库不可达时健康报 `FAILED`、业务端点 **503 而非 13 码**、不承诺「可登录」）与 [25](25-master-key-provenance-and-startup-failure.md) §5.2（那句「可登录」说的是 keyring 坏而库好的场景）。**`25` 的 keyring 故障不拒绝启动、D1/D2/D3/D5–D8 与 §5.3 全部不受影响；`AC-08-F4` / `AC-09-F5` 一字未动。** | [#113](https://github.com/liumingjian/dbs-monitor/issues/113)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [27-v1-deliverables-and-candidate-provenance.md](27-v1-deliverables-and-candidate-provenance.md) · v1 交付物与候选留痕 | **[19](19-agent-distribution-and-upgrade.md) D7 的 `SHA-256 校验和清单`**——原设定「我方随交付物发布清单」，其前提是我方发布二进制；仓库公开、v1 不发任何二进制之后，清单改为**交付团队构建期的副产物**，语义收窄为「plan B 手工搬运的 agent 与 server 正在服务的那份**同源**」，**校验的信任根由我方移至交付团队的构建机**（信任模型改动，非措辞调整）。另**明确不复活** [T15](15-ci-and-release-pipeline.md) D3.3/D5（Environment 审批归档 Release assets）——[18](18-v1-delivery-boundary-bs-binary.md) D10 判其作废后，被归档物本身已消失；其「刻意停顿」改落在打 tag 的动作上并由 `release-gate` 强制。**[19](19-agent-distribution-and-upgrade.md) D1/D2/D5 与「否决代码签名」不受影响；20–24、26 一字未动。** | [#110](https://github.com/liumingjian/dbs-monitor/issues/110)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [29-production-security-boundary.md](29-production-security-boundary.md) · 生产安全边界的具体断言集 | **[26](26-data-and-recovery-gate.md) D7 的执行序全序**——由 `AC-08-S1` → 片条目 → `REC-*` → `AC-08-S7` 改写为 `AC-08-S1` → 片条目 → `REC-*` → **`SEC-*`** → `AC-08-S7`（新增第三横切组的落位，非语义推翻）。另**追加**（非推翻）[14](14-platform-observability-and-diagnostics.md) 的健康子系统：`tls` 为第九个，取值只有 `OK`/`DEGRADED`、**刻意无 `FAILED` 档**（承 [25](25-master-key-provenance-and-startup-failure.md) 追加 `keyring` 为第八个的先例，不原地改 `14`）。**[26](26-data-and-recovery-gate.md) 其余各条、20–24 与 27 两份一字未动；`AC-09-F7` 一字未动。** | [#112](https://github.com/liumingjian/dbs-monitor/issues/112)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [31-real-linux-adaptation-and-final-acceptance.md](31-real-linux-adaptation-and-final-acceptance.md) · 真 Linux 环境适配与最终验收 | **[18](18-v1-delivery-boundary-bs-binary.md) D1/D6 的双架构交付目标**、**[19](19-agent-distribution-and-upgrade.md) D1/D7 的双架构下载允许值与双架构 plan B 备料**、**[27](27-v1-deliverables-and-candidate-provenance.md) 的四个二进制、双架构 `SHA256SUMS` 与 `agent_binary_dir` 必须同时含两架构的要求**、以及 `check-full` / 发布门中 arm64 交叉编译属于 v1 硬门的口径——v1 构建目标收窄为 **`linux/amd64`**，`linux/arm64` 整体移出 v1 归 post-v1 新路线。**[28](28-v1-go-no-go-gates.md) D10「不加 arm64 runner」结论仍成立**，理由收紧为「arm64 已不在 v1 交付目标」。 | [#115](https://github.com/liumingjian/dbs-monitor/issues/115)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [32-platform-storage-watermark-and-write-protection.md](32-platform-storage-watermark-and-write-protection.md) · 外部平台库形态下的平台存储水位与写保护 | **T14 D4 的单一「磁盘水位」保护模型**——外部平台库与 server 本机盘不再混为一个保护对象。平台库容量以 `pg_database_size` ÷ 部署期容量预算形成独立水位并承接紧急拒写新样本；server 本机盘保留为第七健康源，紧急时只停本机大体量写入面。**绝不自动删旧分区、绝不缩短 30 天保留期**保持不变。 | [#125](https://github.com/liumingjian/dbs-monitor/issues/125)（承 [#124](https://github.com/liumingjian/dbs-monitor/issues/124) 拆票前核对） |
| [33-phase-ci-gate-suspension.md](33-phase-ci-gate-suspension.md) · 阶段性停用合入 main 的 CI 门禁 | **[15](15-ci-and-release-pipeline.md) T15「PR 门 `make check`、默认分支 `make check-full`」的执行**、**[10](10-ai-guardrails-and-verification.md) T9 的 CI PR 门执行者**、**[28](28-v1-go-no-go-gates.md) D11 红态修复通道的 CI 前提**——第一个可验收版本完成前,`check` / `check-full` / `acceptance` 三个 workflow 停用、main required status check 摘除;验证责任转移到开发侧真实环境(rexec → mac)红绿闭环,`make check` 全绿的完成定义与 28 号 D9「本地绿不构成 Go/No-Go 证据」不变;恢复条件与动作见该文 §4。 | [#151](https://github.com/liumingjian/dbs-monitor/issues/151)（恢复承接票,保持开放） |

## 5. v1 投产路线新增的决策（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)）

> 本节登记**不取代既有结论、纯新增**的决策文档。取代类记录仍在 §4。

| 文档 | 决策 gist | 决策票 |
|---|---|---|
| [20-v1-acceptance-matrix.md](20-v1-acceptance-matrix.md) · v1 验收矩阵的骨架与判定规则 | 矩阵沿 spec 十片切（片⑩不出条目），页面树与 `operationId` 作覆盖维；每片 `S1` + `F1..F4` 五条基线，加七条横切（四不变式 + 三内置规则，独立计分、必须单测 + 端到端各一）= 52 条硬底，基线不许 `pending`；API 层为默认断言层，浏览器只覆盖 IA §6 五条关键路径 + B6，DB 层只读不写；**测试数据只许经业务 API 或真实采集管线产生**，禁止直插业务表（新增守卫 B11）与 `covered` 漂移门（B12）；载体为本文 + `test/acceptance/matrix.yaml`，执行入口 `make acceptance`（进 `check-full`，不进 `check`），环境为平台库 / 目标库两个 PG + 真 Agent 真实接入；时间参数化不伪造、故障用真实手段不 mock。 | [#111](https://github.com/liumingjian/dbs-monitor/issues/111)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [21-v1-acceptance-entries-a.md](21-v1-acceptance-entries-a.md) · v1 验收矩阵条目 A 组 · 片①⑦⑨ | A 组 **23 条**（片① 8 / 片⑦ 7 / 片⑨ 8），`n-a` 1、加深基线 8。**S1 亲口拍板的验收判据一律落成 `baseline: true` 的加深条目**（全字典对账、待办三态、紧急拒写），硬底算式由「45+7」改为「45+7+逐组加深基线」，**A 组后 60**；能力四态拆三条路径（`MISSING` / 整份 `UNKNOWN` 不得反推 / `NOT_APPLICABLE` 不得冒充 0）；IA §6.5 整条路径由 `AC-01-F2` 一条承载；**journal 载体明确不在 `make acceptance` 内验证**（裸进程无 systemd，载体真实性移交片⑩，写进矩阵不藏）；片⑨ `F2` 记 `n-a` 由 `F6` 补回并补 `F7` 收证书过期注入；时间参数化取值表定稿（分区跨度 1min 是外溢实现的唯一硬要求，单轮 ≤10 分钟）；`test_ref` 须内含条目 ID 字面量使 B12 退化为单条 grep；`operations` 预写 12 个尚不存在的 `operationId`，使缺口报告不把「还没做」伪装成「没缺口」。 | [#118](https://github.com/liumingjian/dbs-monitor/issues/118)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [22-v1-acceptance-entries-b.md](22-v1-acceptance-entries-b.md) · v1 验收矩阵条目 B 组 · 片②③④ | B 组 **23 条**（片② 6 / 片③ 9 / 片④ 8），`n-a` 2、加深基线 7、普通加深 1（全矩阵唯一），**硬底 60 → 67**。**通知端到端断到真收端**——compose 增本地 SMTP sink + Webhook 回环接收端两个进程外真实对端，断真投递与 HMAC 签名头，否决「只断投递记录与三次退避终态」（出口一断就绿，正是反假覆盖要根除的形态）；`F3` 在片④读作「外部依赖不可用」，语义映射记账不默默扩义；四条 `F4` 手段同源断言点不同全部保留，`AC-04-F4` 是「待发通知落表而非内存队列」的唯一验收面；**三处参数化**（`repeat_interval` 下限 30s、快照截断 100→5、告警历史保留 90 天→2min）与**两处坚决不参数化**（`NO_DATA` 门槛 2 周期、退避 3 次——那是「平台固定不可配」的语义本身）；片② `S1` 用真开 5 条连接推阈值，否决用内置规则触发（会绕过规则 CRUD 一半）；IA §6.4 由 `AC-03-S3` 一条承载。外溢实现硬要求两条：repeat 下限可配、快照截断上限可配。 | [#119](https://github.com/liumingjian/dbs-monitor/issues/119)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [23-v1-acceptance-entries-c.md](23-v1-acceptance-entries-c.md) · v1 验收矩阵条目 C 组 · 片⑤⑥ | C 组 **15 条**（片⑤ 10 / 片⑥ 5），`n-a` 1、加深基线 4，浏览器执行 7 次（全矩阵最密），**硬底 67 → 71**。IA §6.1/§6.2/§6.3 各挂一条浏览器条目，否决合并与「一条页面树大走查」；**「13 码后端真实驱动」拆两条**——`AC-05-F2`(api) 管码的广度（片①能产出的 **11 码**逐码真实驱动、200+ 码不走 error、不补 0）、`AC-05-F5`(browser) 管 UI 覆盖，「11 码不是漏两码」的对账三句写死在 `reason` 里；**AntD 观感门 = `AC-05-S4` 记 `n-a`**，判定归 #115、卡顿即触发 `11` §9 二类停下不得自行换库；**归并全语义单设 `AC-05-S5`**（纯函数单测证明不了它被接进投影路径，入参跨片②③④⑦，是全矩阵准备成本最高的一条）；**背压—`NO_DATA` 张力显式记账**（`12` D7 口径优先于 T14 一般表述，`AC-06-F4` 显式不断 `NO_DATA` 豁免）；**片⑥ `F1` 不记 `n-a`**，「本页无写语义 `x-required-role`」是「无写面」决策的唯一验收面。C 组零新增长杆、零外溢实现硬要求。 | [#120](https://github.com/liumingjian/dbs-monitor/issues/120)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [24-v1-acceptance-entries-d.md](24-v1-acceptance-entries-d.md) · v1 验收矩阵条目 D 组 · 片⑧ + 横切组 | D 组 **20 条**（片⑧ 13 / 横切 7），**硬底 71 → 78；四票条目定稿完毕，`matrix.yaml` 自此无 `TBD`。全矩阵终态 81 条条目、78 条硬底、`n-a` 5、`pending` 2。** **接入前置 = 业务 API 登记直起 agent 真进程**（安装命令 / CA 指纹钉扎 / unit 写入 / 安装期时钟自检移交 #110——compose 无 systemd，跑真实安装脚本必然退化成「一半步骤被跳过」的假动作）；**横切组落成「自带断言集 + 搭车执行」并新增 `rides_on` 字段**（达标由横切自己的 `test_ref` 证明、与承载执行是否绿无关），否决「各自独立执行一次」与「就是别片的别名」；**A 栏新增 `A10`/`A11`**（B 栏扫 schema 只证明「声明齐全」，把所有端点声明成 `viewer` 也能全绿，顶不了表驱动判定）；`F2` 记 `n-a`、`VERSION_UNSUPPORTED` 归 `F3`（接入即拒是写操作失败分类不是空状态码，本片使该码有了唯一产生者）；**「凭据永不回显」三层分工**（B7 管 schema、B3 管声明、端到端管响应体**全文正则**——防的是意料之外的字段）；**执行序两条硬约束**（`AC-08-S1` 最先、`AC-08-S7` 主密钥轮换最末）。**`exceptions` 全程 `[]`**（四组无一条需要绕过业务 API，矩阵最强的一条自证）。外溢实现硬要求三条：`10` §3.2 登记 `A10`/`A11`、采集新鲜度阈值可配、轮换命令可非交互调用。 | [#121](https://github.com/liumingjian/dbs-monitor/issues/121)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [26-data-and-recovery-gate.md](26-data-and-recovery-gate.md) · 数据与恢复门的具体证据 | 恢复门落成矩阵上**第二个横切组 `REC-1..10`**，全部 `baseline: true`，硬底 **78 → 88**、条目 81 → 91（`n-a` 仍 5、`pending` 仍 2、`exceptions` 仍 `[]`）；否决拆进九片（达标判定退回人肉汇总）与完全不进矩阵（逃出自动判定即退回人工检查表）。**启动失败按失败性质而非启动阶段两分**：连不上平台库不拒启动（HTTP 起、健康 `FAILED`、业务端点 **503 而非 13 码**、库回来后**后台补跑迁移自愈**），迁移**执行**失败拒绝启动（非零退出、日志指名笔次、不得降级成 503 继续运行）。**场上三把平台库 advisory lock 互相独立且行为不同**——rotate 拒绝、迁移**等待**、进程**立即拒启动**；合成一把会让「正在迁移」与「已经在跑」退化成同一句错误信息；一手事实：goose v3.24.3 已有 `WithSessionLocker` 而 `migrations/migrate.go` 未启用（裸并发）。**备份恢复断到「灌进一个真空 PG」**（否决同实例 `createdb`：共用 initdb 参数/locale/扩展/角色会掩盖「库是自描述的」最易翻车处），门槛是**语义覆盖而非行数**（加密凭据 / 含版本快照的规则 / 未恢复实例+事件 / 样本跨 ≥2 分区），数据全由片条目跑完后自然存在；`13` D7/D8 的 keyring 分离**正反两断**，反面与 `AC-08-F4` 按场景来源划界（运行中 vs 恢复链）。重启恢复三项进断言、**连接池刻意不进**（无外部可观测语义，写进 `reason` 防误读），server / agent 分开两条；分区**只收肯定面**（预建 7 跨度、`DROP` 非 `DELETE` 对账 `pg_class`、维护失败进健康事实源），否定面已由 `AC-09-F5` 断死不重复；`REC-9` 用**维护间隔配成极大值**制造无匹配分区（参数化不是模拟，否决 test-only 开关与 DB 层手工 `DROP`）。**REC 组一律独立执行、`rides_on` 恒 `[]`**（注入会改变现场），新增 `after` 字段记执行序依赖，不与 `rides_on` 混用；执行序全序：`AC-08-S1` → 片条目 → `REC-*` → `AC-08-S7`。客户责任清单**七条 + 两条措辞硬要求**作为**给 #116 的单向输入**，本票不建第二份文档。外溢实现硬要求五条（goose session locker + 超时可配、进程锁、分区维护间隔可配、库不可达 503 + 后台补跑迁移自愈、迁移失败非零退出不得降级）与环境要求一条（`compose.yaml` 新增 `restore-target` 独立 profile）。 | [#113](https://github.com/liumingjian/dbs-monitor/issues/113)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [30-external-postgres-prerequisites.md](30-external-postgres-prerequisites.md) · 外部前置 PostgreSQL 的版本要求与部署前置条件 | 填 [18](18-v1-delivery-boundary-bs-binary.md) D2 留下的洞，结案其「版本要求另议」并接下 §4 第 3 点移交的四件事。**平台库钉死 PG 17**（17.x 任意小版本），主版本不符**拒绝启动**、**不设任何逃生舱**——决定这条的不是技术下限（PG14）而是 sqlc 解析器固定 PG17 这一护栏漏洞：支持 14–17 区间会让 `make gen` 的静态保护结构上为假，补回来只能真跑四版本 × 94 条。**独占边界承诺面写实例级、断言面写 database 级，差额显式记账**（平台是普通角色，断得死 database + 独立 schema，断不死同实例的邻居与别人的 DBA），否决「承诺面降到 database 求说到做到」与「抬到实例级做洁净度检查」。**启动前置校验按「能否自愈」而非启动阶段归位**（与 [26](26-data-and-recovery-gate.md) D5 同源并延伸）：主版本 / 编码非 UTF8 / 无 `CREATE` / **schema 内有非平台对象**（拦「配置指错到客户业务库」，比版本更能救命）/ **TLS 未生效** → 拒启动；locale、时区、同实例多库、小版本落后 → warn + `DEGRADED` 不拒启动。**撤回 #116 正文的「可用磁盘」一项**——外部形态下平台看不到主机可用空间，只进客户责任清单（≈49.1 GB）。**补上 `26` 自愈路径的那道缝**：库回来后校验失败 → **不退出**、`FAILED` 指名项、退避重试。**独立 schema `dbsmon` 不用 `public`**（`search_path` 钉在连接串，SQL 一句不改），**最小权限 = schema owner，不要 superuser、零扩展**（写成对客户 DBA 的正式承诺）。**TLS 两处都断**：配置期拒收弱于 `verify-full` 的 `sslmode`，运行期查 `pg_stat_ssl`（`prefer` 会静默降级成明文，而平台库密码正明文躺在 `0600` 配置文件里）。大版本升级三条承诺，其中**「升到 18+ 会导致平台拒绝启动」必须正面写死不得软化成建议**（新增第三条措辞硬要求）。**托管 PG 不禁止、不背书**（硬门拦不住它，但 `REC-3` 的恢复承诺在托管形态下不适用；不加检测，识别不可靠且是猫鼠游戏）。**开发与 CI 触及的一切平台库必须是 17**——原则归本文、落地归 #114。客户责任清单 = `26` §9 七条原样接收 + 本票五条（专属实例 / initdb 与运维归客户 / 磁盘平台断不了 / 升 18 起不来 / 不需要 superuser），成文归 #110。矩阵**追加 `REC-11..13` 进既有 `REC` 组**（不开第三组），硬底 **88 → 91**、条目 91 → 94（`n-a` 仍 5、`pending` 仍 2、`exceptions` 仍 `[]`）；`REC-11` 断「必须退出」、`REC-13` 断「必须不退出」，互斥不合并。外溢实现硬要求六条 + 环境要求一条（`compose.yaml` 增非 17 平台库 profile）。**不 supersede 任何在效条款**；只记账 [04](04-metric-storage-model.md) §7.4 悬空指向（T8 D4 已作废）改指本文，不原地改 `04`。 | [#116](https://github.com/liumingjian/dbs-monitor/issues/116)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [27-v1-deliverables-and-candidate-provenance.md](27-v1-deliverables-and-candidate-provenance.md) · v1 交付物与候选留痕 | **候选身份 = 40 位 commit SHA，语义化 annotated tag 是别名**；版本经 `-ldflags -X` 注入并加 `--version`（此前全仓无版本号，`19` D2 的「Agent 落后 warn / 超窗拒收」一直没有被承诺物），超窗 = **MAJOR 落后 ≥2**（由 T3 兼容一个大版本推出）。**仓库公开，v1 不发布任何二进制、不产 Release assets**，承诺收窄为「给定 SHA + `.tool-versions` 工具链可构建出功能等价产物」，**明确不承诺 bit-for-bit 可复现**（前端产物链未锁定，承诺测不了的事等于立假门）。交付物**两层**：规范层 = tag 指向的仓库快照（双配置样例、双 unit 模板、部署前置条件文档、plan B 手工接入文档、`docs/deploy/build.md`），产物层 = 交付团队自建的四个二进制 + 构建期 `SHA256SUMS`，我方不归档不承诺内容；**CA 证书文件不进交付物**（等于发布共享信任根），迁移已 `go:embed` 天然不是交付物；**死资产删除不留 legacy**（`scripts/package-linux.sh`、`packaging/bundle/`、`dbs-monitor-postgres.service`）。`main` 只开 required status check `check` + 禁 force push/删除 + **管理员不豁免**，**不要求 approval 且写成有理由的决策**（单维护者下作者不能自批，要求 approval 的真实结局是管理员 bypass——把被绕过的规则伪装成生效的规则）。**Go/No-Go 报告 = `make acceptance` 生成的 `acceptance-report.json`**（头部四字段 + 逐条目状态 + 顶层 `verdict`，**判定规则写死在生成器里而非报告里**）入库 `docs/validation/`，另有薄壳 Markdown 承载真机观感与 `AC-05-S4`，**不得覆盖 `verdict`**；tag 打在**候选 SHA** 上，新增 **`release-gate` workflow**（tag push 触发，只校验不构建：`check-full` 成功 + 报告存在 + annotated；正式 tag 要 `verdict: GO`，**`-rc.N` 只要求「被正式评判过」**——否则真机多轮必然逼出「为了不打红 tag 而不打 tag」）。`agent_binary_dir` 部署时放入双架构 agent，**缺失时下载端点明确失败而非 404 沉默**并进健康事实源（否决 `go:embed` 进 server）。升级/降级承诺：v1.x 可直升、跨 MAJOR 逐版、**降级不受支持**、先 server 后 agent、v1.0.0 首发无 0.x 升级路径，两句措辞硬要求（「降级不受支持」「未备份即升级 = 不可回退」）不得软化。**对矩阵只修订 `AC-08-S8` 一条判据，91 条条目 / 88 条硬底一字不变**；**不建交付前检查表**（第二份真相，勾选表归 #114 且应由报告承载）。外溢实现硬要求八条。 | [#110](https://github.com/liumingjian/dbs-monitor/issues/110)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [29-production-security-boundary.md](29-production-security-boundary.md) · 生产安全边界的具体断言集 | 把地图 Notes 第 6 条（安全头无条件输出、server 自终结 TLS）从「边界存在」写成「边界内容」。**安全头六项取值定稿**：CSP 的 `script-src 'self'` 严格、`style-src` 放行 `'unsafe-inline'`（一手事实 `antd` 6.5.3 经 cssinjs 运行时注入 `<style>`；**否决全 nonce 化**——它要求逐请求改写 `go:embed` 出来的 `index.html`，为样式侧收益拆掉静态直发层，代价「样式侧注入面未关」显式记账）；**HSTS 只发 `max-age=31536000`，永不发 `preload`**（替客户域名进浏览器内置不可逆名单是事故）**也不发 `includeSubDomains`**（不拥有兄弟子域），并写明「**无条件输出 ≠ 取最激进值**」。**中间件必须包在 `/api/` 前缀分叉之前的最外层**——现状 `cmd/monitor-server/main.go` 是两条 handler 链，只挂一条的症状极隐蔽（页面能开、接口能通、只扫 API 报绿）。**TLS 下限 1.2 → 1.3**（现状 `MinVersion` 是 Go 默认值不是决策），**密码套件不列举**（列了就是承诺一份会随 Go 版本过时的清单），换证走重启不做热重载。**证书过期不拒启动**（与 `25` D4 同构：拒启动会把计划内的换证逾期升级成「监控平台整个看不见」），`tls` 降级、<30 天预警同档但事件可区分；与 `AC-09-F7` 同注入手段、两个断言点，**不合并**。**会话 = 服务端会话表而非无状态 JWT**（`17` D2 的「停用立即失效」逼死无状态方案），`__Host-` 前缀 + `HttpOnly`/`Secure`/`SameSite=Strict`，绝对 12h／空闲 2h **两者必须可配**，**并发会话不限制**（限制会在双人值班或一人两处盯屏时把人锁在门外，而这是监控平台），口令自改与被重置吊销其它会话；**不引入 CSRF token**（`SameSite=Strict` + 全部写操作走 JSON 必过预检）**并把推理写进文档**——「没做 CSRF」和「忘了做 CSRF」在代码上长得一模一样。**审计不建 `audit_log`**（与各片已冻结的 actor 列构成双写，双写必然漂移）**也不用 journal 承载**（`14` D4 下是可丢载体，扛不起「可归因」硬约束），产出**十一项「必须可归因的写操作登记表」**、可读者 = 全部登录用户只读（不新造审计员角色，否则立刻要求实例级授权配套）；守卫拆两层——**A 栏新增 `A12`** 管完整性（漏没漏登记一项是表驱动判定不是执行判定），端到端只跑登录失败／用户停用／凭据更新三种最易漏形状。**最小权限本票只出「server 以非 root 专用用户 `dbsmon` 运行」一条**，平台库那一半显式引用 [27-external-postgres-prerequisites](30-external-postgres-prerequisites.md) 不复写。**`govulncheck` 进 `check-full` 且阻断、`npm audit` 只报告不阻断、两者都不进矩阵**（矩阵是运行期验收），联网张力处置为「`check-full` 允许联网、`release-gate` 只读报告不重跑」——否则离线构建机上退化成「跳过即绿」，一条被绕过却看起来生效的规则比没有规则更坏。矩阵**新开第三个横切组 `SEC-1..10`** 全基线、`rides_on: []`，**硬底 91 → 101、条目 94 → 104**（`n-a` 仍 5、`pending` 仍 2、`exceptions` 仍 `[]`）；否决并进 `REC`（组名语义是恢复门，不同轴会让组名说谎）与拆散挂 `AC-08`（安全头与会话跨全部九片）。新增守卫 **`B13` 安全头 golden 快照**（与 `A9` 同构；`B13` 断取值逐字、`SEC-1` 断两条链都发）。验收手段两处坚持：证书过期**真放一张过期证书**（不加 test-only 开关，承 `26` D9）、会话时限**参数化不模拟**（90s/30s 跑同一段代码，同 `22` 对 `repeat_interval`）。外溢实现硬要求七条 + 环境要求两条（compose 中 server 必须以非 root 跑，否则 `SEC-9` 恒绿是自欺条目；`test/acceptance/fixtures/` 预置两张证书）。**另记编号撞车**：`docs/design/` 现存两份 27（`9c5db89` 与 `6692f51`），本文避开取 29、28 空缺，重编留作待处置项。 | [#112](https://github.com/liumingjian/dbs-monitor/issues/112)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [28-v1-go-no-go-gates.md](28-v1-go-no-go-gates.md) · v1 Go/No-Go 质量门组成 | 把 20–24／26／27／27-ext／29 定稿的东西接进真实执行者。**开发者面对的命令仍只有两条**，`make acceptance` 与 `release-gate` 定性为非开发者入口，[10](10-ai-guardrails-and-verification.md) D1「不许加第三层」**不 supersede**（它约束的是开发者的判断题数量，不是 CI job 数）；**104 条矩阵每次 push `main` 全量跑、红即阻断，但在 CI 里是独立 job**（塞进 `check-full` 会爆 30 分钟 timeout 且失败时分不清是编译还是第 71 条），否决子集/采样/只人工跑，并写死「撑不住时先接受时长、不砍覆盖面」；**被监控 PG13–17 只跑采集与能力探测子集**（告警、通知、权限与目标库版本无关），跑在手工发起的发布门，必须产出「每版本 × 每能力」三态表——那是 [23](23-v1-acceptance-entries-c.md) 记下的采集侧版本差异**唯一**的验收面；**新增守卫 `B14`**（编号让给先落地的 [29](29-production-security-boundary.md) `B13`）：既查连上的库是 17、也扫 `compose.yaml` 与 workflow 的平台库 image tag，并落地 `10` §2.1 一直是纸面的 `datlocprovider <> 'i'`——运行期拒启动保护客户，保护不了「会话在 PG16 上写 PG17 语法而 `make gen` 全绿」，结清 [27-ext](30-external-postgres-prerequisites.md) D7；**RT-C 定为记录门不是阈值门**（无基线数据的阈值必然被调到绿，比没有门更坏），但**缺数据即 NO-GO**；**三份证据合成唯一 `verdict`**：`evidence` 三块、判定规则仍写死在生成器里，**任一块缺失 → NO-GO、任一块 `candidate_sha` 与顶层不一致 → NO-GO，且两者必须给出不同的失败信息**（防多轮真机验收演化成「贵的证据只跑一次」）；**新增 `release-evidence` workflow，`workflow_dispatch` + SHA 入参**，一般化为「一切证据类 workflow 以 SHA 为入参手工发起」（tag 触发会造环形依赖，定时跑打的是移动靶）；**GitHub runner 上的绿不构成 Go/No-Go 证据**——强制 `provisional: true`、`verdict` 永不出 `GO`、报告不入库 `docs/validation/`（runner 无 systemd、cgroup 分母被限，`REC-*` 与权限类条目在其上跑绿说明不了任何事），本票只给这一条判据，真机清单归 #115；不加 arm64 runner（只会得到更多不算数的绿）；**`main` 允许红**（required check 仍只有 `check`，提为 required 的真实结局是直接 push 绕开 PR），兜底靠 `release-gate`；**快层 ≤120 秒预算一字不改**、`make acceptance` 绝不进 `check`；**登记表落位**：B 栏加「跑在哪层」一列，不开 C 栏（会与门总表构成第二份真相），随文补登 `A10`/`A11`/`A12`/`B11`/`B12`/`B13`/`B14`；**门总表 G1–G8 是唯一载体**，不建任何 Markdown 勾选表、不进 `matrix.yaml`（兑现 [27](27-v1-deliverables-and-candidate-provenance.md) 的「不建第二份真相」）。**对 [29](29-production-security-boundary.md) D7 让步**：漏洞扫描归属以其为准，本票只定「不进 `evidence`、不参与 `verdict`」——其「`release-gate` 只读报告不重跑」已解掉本票原本担心的 CVE 追溯阻断。对 [15](15-ci-and-release-pipeline.md) D2 **只增不改**（现有四行没有一行是错的）。矩阵条目一条不动。外溢实现硬要求九条 + 环境要求一条。 | [#114](https://github.com/liumingjian/dbs-monitor/issues/114)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
| [31-real-linux-adaptation-and-final-acceptance.md](31-real-linux-adaptation-and-final-acceptance.md) · 真 Linux 环境适配与最终验收 | v1 真 Linux 最终验收规范（票 resolution 的载体补登，内容与票面一致）：正式兼容性证据只认 **Kylin Linux Advanced Server V10 AMD64 KVM**（实查画像 glibc 2.28 / systemd 243 / cgroup v1），正式轮次须取得独占验收窗口；**每个被评判的候选 SHA 真机重跑完整证据**——干净 checkout 构建并校验版本与 SHA-256、`make check-full`、104 条矩阵、被监控 PG13–17 采集与能力探测子集（每版本 × 每能力三态表）、RT-C 记录门（缺数据即 NO-GO、不设阈值）、宿主机原生 systemd 段、`AC-05-S4` 人工观感，汇成唯一 JSON `verdict`；**正式 `v1.0.0` 须同一 SHA 连续两轮完整 GO**，轮间重置环境不换主机、昂贵证据不得只跑一次、失败即连续计数归零；同一轮 = Docker 证据段 + 原生 systemd 段（否决 systemd-in-container）；**预检失败记 `INVALID` 不产生 GO/NO-GO，候选启动后任一门失败即 `NO-GO`** 不得以「环境抖动」豁免；glibc 以 `CGO_ENABLED=0` + ELF/`ldd` 证明不链接 libc、不承诺 glibc 下限；时钟偏差超 ±5s 拒绝启动；只验 cgroup v1，SELinux/AppArmor 只记录不宣称兼容；证据归档为 `docs/validation/v1-go-no-go-<shortSHA>.json`（六类证据 + 环境指纹，归档前过秘密扫描），机器 verdict 是唯一结论。 | [#115](https://github.com/liumingjian/dbs-monitor/issues/115)（地图 [#105](https://github.com/liumingjian/dbs-monitor/issues/105)） |
