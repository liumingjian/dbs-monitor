# 前端重构：以 IBM Carbon 重建监控控制台界面

## Problem Statement

作为一个自托管的 PostgreSQL 监控平台，dbs-monitor 的前端目前是一套没有设计系统的界面：全站只有一个约 110 行的全局样式表，颜色是散落在组件里的硬编码字面量，另有近百处内联样式；组件库用的是 Ant Design 的默认主题，从未做过任何主题定制。

这带来三个具体问题：

1. **看不出严重程度。** 一个监控控制台的首要职责是让 DBA 一眼判断"哪里出事了、有多严重"。当前界面没有统一的状态色语汇，严重度靠零散的标签颜色表达，而这些颜色从未经过对比度校验。
2. **数字不对齐。** 表格里的数值用的是比例字体，列与列之间无法快速比较——这是数据密集型界面的基本功。
3. **没有可演进的基础。** 因为没有 token 层，任何一次视觉调整都是全局搜索替换；因为没有展示组件层，同一种面板在不同页面被实现了多次。

团队已经产出了一份完整的视觉规范（仓库根目录的 `DESIGN.md`，基于 IBM Carbon Design System，含专门为监控产品编写的 "Product Surfaces" 一节），以及一套静态 HTML 视觉原型。当前实现与这份规范之间没有任何对应关系。

## Solution

用 IBM 官方的 Carbon 设计系统重建整个前端：以 `@carbon/react` 替换 Ant Design，以 `@carbon/charts` 替换图表实现，按 `DESIGN.md` 建立 token 层与展示组件层，并把导航从顶部条改为 Carbon UI Shell 的「炭黑页头 + 可折叠侧栏」形态。

对使用者而言，结果是：

- 严重度有统一且通过 AA 对比度校验的色彩语汇；蓝色只表示"可交互"，从不表示状态。
- 所有数值以等宽字体的表格数字呈现，列对齐。
- 表格有一致的行高（标准 40px / 密集 32px）、粘性表头、行首严重度色条。
- 导航从"顶部四个入口"变为"监控 / 告警 / 管理"三组侧栏，可折叠成图标栏并记住状态。
- 界面在 1280px 最小支持宽度下不横向滚动，也不丢列。

功能行为保持不变。这是一次视图层的替换，不是一次产品重设计。

## User Stories

### DBA / 值班工程师

1. 作为值班 DBA，我希望实例列表里每一行的严重度以固定位置、固定颜色的色条呈现，这样我扫一眼就知道该先看哪一行。
2. 作为值班 DBA，我希望严重度不只用颜色表达，每个状态点旁边都有文字标签，这样我在色觉受限或屏幕偏色时依然能判断。
3. 作为值班 DBA，我希望表格里的连接数、QPS、慢查询数、复制延迟等数值右对齐且等宽，这样我能直接纵向比较各实例。
4. 作为值班 DBA，我希望表格表头在我向下滚动时保持可见，这样我在第 40 行也知道自己在看哪一列。
5. 作为值班 DBA，我希望在 1280px 宽的笔记本上打开实例列表时不需要横向滚动，也不会有列被隐藏。
6. 作为值班 DBA，我希望能把表格切换成密集模式，在同样高度里看到更多行。
7. 作为值班 DBA，我希望指标图上的告警阈值以虚线标出并带数值标签，这样我能立刻看出当前值离阈值还有多远。
8. 作为值班 DBA，我希望绘制百分比类指标（如缓冲区命中率）的图表不要从 0 画到 100，否则曲线是贴在顶部的一条直线，什么也看不出来。
9. 作为值班 DBA，我希望数据系列的配色里不出现红色，这样我不会把一条普通曲线误认为告警。
10. 作为值班 DBA，我希望实例列表每行末尾有一个趋势缩略图，让我不用点进详情就能看出走势。
11. 作为被夜里叫醒的 DBA，我希望告警流页面在手机上可用（单列布局），这样我能在赶到电脑前先判断严重程度。
12. 作为 DBA，我希望刷新后发生变化的数值有一次短暂的高亮，这样我知道哪些数字动了。
13. 作为 DBA，我希望界面上没有数字滚动、图表描边生长这类动画，因为我要盯着它看一小时。
14. 作为 DBA，我希望加载中的区域显示骨架占位而不是整页转圈，这样已经加载好的部分我可以先看。

### 导航与信息架构

15. 作为使用者，我希望左侧有一个按"监控 / 告警 / 管理"分组的导航，这样我知道整个系统有哪些能力。
16. 作为使用者，我希望能把侧栏折叠成图标栏，把横向空间让给宽表格。
17. 作为使用者，我希望折叠状态在我切换页面后依然保持，不用每次重新折叠。
18. 作为使用者，我希望折叠后把鼠标停在图标上能看到它的名称。
19. 作为使用者，我希望折叠后仍能看到未读告警的提示（图标角上的小圆点），不会因为折叠而漏掉。
20. 作为键盘用户，我希望折叠按钮能被聚焦、能被读屏软件读出当前是展开还是收起。
21. 作为使用者，我希望原本分散在多个页面的同类功能（如慢查询的三个视图、告警设置的四个页面）被收拢到一个页面的多个标签页里，减少我在导航里找路的次数。
22. 作为收藏过旧链接的使用者，我希望旧的页面地址仍然能把我带到正确的位置。

### 表单与操作

23. 作为管理员，我希望新建实例、编辑告警规则等表单在我填错时，错误提示出现在对应字段下方而不是页面顶部。
24. 作为管理员，我希望服务端返回的字段级错误也能定位到对应的输入框。
25. 作为管理员，我希望编辑告警规则时能在抽屉里编辑，同时还看得见规则列表。
26. 作为键盘用户，我希望打开抽屉或对话框后焦点被限制在里面，按 Esc 能关闭。
27. 作为管理员，我希望危险操作（终止会话、删除规则）的按钮以醒目的红色呈现，且需要二次确认。
28. 作为只读用户，我希望我无权执行的操作以禁用态呈现并说明原因，而不是点了之后报错。
29. 作为管理员，我希望配置维护窗口时能同时选择日期和时间。
30. 作为使用者，我希望能一键复制实例连接串、SQL 指纹等长文本。

### 视觉一致性

31. 作为使用者，我希望所有按钮、卡片、输入框都是直角的，界面看起来是同一套东西。
32. 作为使用者，我希望页面上唯一的蓝色出现在我可以点的东西上，这样我不用猜什么能点。
33. 作为中文使用者，我希望中文正文不使用细体，因为小字号的细体中文会糊成一团。
34. 作为使用者，我希望界面在没有外网的环境里字体依然正确显示。
35. 作为使用者，我希望过长的内容被截断并带省略号和悬停提示，而不是把等宽的 SQL 文本从中间折行。

### 开发者与维护者

36. 作为前端维护者，我希望所有颜色都通过语义化的设计令牌引用，这样将来加深色主题是一次覆盖而不是一次重写。
37. 作为前端维护者，我希望有一层无业务含义的展示组件可以复用，而不是在每个页面重新拼装面板。
38. 作为前端维护者，我希望展示组件和有业务含义的领域组件分居不同位置，各自的职责边界清晰。
39. 作为前端维护者，我希望测试通过角色、标签和稳定的测试标识定位元素，而不是通过组件库生成的类名，这样换库不会让测试全红。
40. 作为实现本规格的 agent，我希望有一层已经建好的基线组件和令牌可用，这样我在改页面时不需要发明新的视觉模式。

### 部署与合规

41. 作为私有化部署的运维人员，我希望前端不在运行时向任何外部域名发起请求。
42. 作为负责构建的工程师，我希望依赖安装过程中的遥测上报被显式关闭，且这件事写在构建脚本里可被审计。
43. 作为发布负责人，我希望最终二进制的体积增长在可接受范围内，并且这个数字是实测的。

## Implementation Decisions

### 组件库

- 移除 Ant Design 及其图标包，改用 **`@carbon/react`**（1.115.x，官方支持 React 19）。
- 图标改用 **Carbon 官方图标**，以内联 SVG 的形式集中在展示组件层内，覆盖当前使用的 34 个图标位。不引入完整图标包的 npm 依赖之外的额外抽象。
- 不引入 `@carbon/ibm-products`。核心库缺少的 Drawer / SidePanel 自行实现（复用 Carbon 的层级与焦点管理原语）。为一个抽屉引入一个带十余个传递依赖并锁定核心库版本的产品级包，代价不成比例。

### 样式与构建

- 必须走 **Sass 构建**，不使用预编译 CSS。原因有二：预编译 CSS 把 Plex 字体的 `@font-face` 硬编码指向 IBM 的 CDN，在无外网的私有化部署中会静默降级为系统字体；且它不把字体族发布为 CSS 自定义属性，导致中文回退栈无法覆盖。
- 引入 `sass-embedded`。Vite 配置使用现代 Sass API 的加载路径选项（官方文档给出的是已被 Vite 7 废弃的旧选项，不可照抄），并静音 Carbon 触发的大量 dart-sass 弃用警告。
- 接受 Sass 带来的开发服务器热更新变慢。
- 开启 `skipLibCheck`。`@carbon/react` 自带的类型声明无法通过严格检查，这是官方建议；该选项没有按包生效的粒度。

### 设计令牌

- 令牌以 Carbon 的 Sass 主题映射为基础，**在冲突处以 `DESIGN.md` 为准**，通过主题映射覆盖，不 fork 上游。已知需要覆盖的至少三处：
  - 警告色取 `DESIGN.md` 规定值而非 Carbon 默认的黄色。此处不是审美偏好：Carbon 默认黄在白底上的对比度约 1.68:1，橙色候选约 2.46:1，均不满足文本 4.5:1 与图形 3.0:1；`DESIGN.md` 选定值实测 5.19:1。监控产品需要把警告色用作 8px 状态点和越界数值的**前景**，因此必须过线。Carbon 官方指引本身也说该黄色是背景色而非前景色。
  - 次级文字色取 `DESIGN.md` 规定值。Carbon 的默认次级灰属于占位/禁用档位，用于表格副标签和图表轴标签时对比度不足。
  - 数据可视化色板沿用 `DESIGN.md` 的六色，其中刻意排除红色系，避免数据系列与严重度色混淆。
- 蓝色只表示可交互（链接、主按钮、选中导航项、选中标签下划线、焦点环），永不表示状态。状态色只表示状态，从不作为大面积背景填充。唯一例外是破坏性操作按钮。
- 所有颜色以语义令牌引用，不出现字面量色值。深色主题**本次不实现**，但令牌结构必须保证将来只需一次覆盖。
- 顺带修正 `DESIGN.md` 内部的一处自相矛盾：禁忌一节写的替代色与令牌表及其对比度论证不一致，应统一为令牌表中的值。

### 字体

- 关闭 Carbon 自带的字体声明发射，改为手工放置 IBM Plex Sans（300/400/600）与 IBM Plex Mono（400/500）的 Latin 子集 woff2，随构建产物一同嵌入二进制，约 148KB。不使用完整的 Plex 包（会带入用不上的字重与衬线体）。
- 通过 Sass 覆盖字体族配置，在 Plex 之后追加中文回退栈。Carbon 不提供任何 CJK 字体，这部分完全自理。
- 中文正文使用常规字重，细体只用于 28px 及以上的中文标题。Carbon 的正文字距只施加于拉丁文本，不施加于等宽数值（会破坏列对齐）。

### 图表

- 面板级图表改用 **`@carbon/charts`**，阈值线使用其原生的阈值配置能力。
- **表格内的趋势缩略图手写内联 SVG**，不使用图表库。该库没有缩略图组件，官方替代做法是把折线图关掉坐标轴、图例和网格来假装，对一个几十像素宽的行内图元过重。
- **移除 echarts**。不保留两套图表实现，避免两种图表美学并存。
- 现有图表模块承载的阈值线、严重度配色与数值本地化格式化需在新库上重建；其纯函数部分（选项推导、格式化）保持可独立测试。

### 表单

- 引入 **react-hook-form + zod** 承接表单状态与校验。Carbon 只提供字段级的 `invalid` / `invalidText`，没有表单级 API。
- **API 错误映射模块的契约需要变更**：当前它把服务端字段错误映射成 Ant Design 表单的错误数组形状；改为映射成 react-hook-form 的字段错误设置形式。这是本次唯一一处 UI 层之外的接口变更。
- zod schema 与 OpenAPI 生成的类型对齐，使校验规则能被类型系统覆盖——这是相对当前 `rules` 数组的净改进。

### 表格

- 使用 Carbon 的数据表格。行高档位直接对应规范：标准行取 40px 档，密集行取 32px 档，无需自定义。
- **移除全部横向滚动与固定列**。Carbon 的数据表格不提供固定列，而 `DESIGN.md` 已把这个问题当作设计问题解决过：在支持宽度内应通过收窄列的最小宽度来容纳，绝不丢列、绝不在 1280px 横向滚动；只有在低于最小支持宽度时才恢复滚动条。过宽内容以省略号加悬停提示处理。
- 粘性表头与横向滚动容器不能是同一个元素（滚动容器会成为粘性定位的参照，导致表头落在表格内部）。在支持宽度内表格不作为滚动容器；低于支持宽度时表头改为粘附于滚动容器。

### 布局与信息架构

- 页头改为 Carbon UI Shell 的炭黑 48px 页头；侧栏 256px，分「监控 / 告警 / 管理」三组，可折叠为 48px 图标栏。
- **折叠是一个真实状态而非装饰**：折叠时标签与分组标题从布局中移除而非淡出（否则每个标题会留下一条空带），标签以悬停提示保留，未读计数以图标角标保留；状态持久化，在存储不可用时降级为不记忆；切换控件带展开状态的无障碍属性与说明性标签。
- **允许合并路由**，但不得丢失任何功能点。每一次合并需单独说明合并了哪些入口、旧地址如何重定向。候选合并方向：慢查询相关的多个视图收拢为一个多标签页面；告警设置的多个页面收拢为一个多标签页面。
- 规范未覆盖的页面（登录、告警详情、性能事件、维护窗口、404 等）由实现者基于基线组件推导，**不得发明新的视觉模式**。
- 桌面优先，最小支持宽度 1280px。页面内边距在超宽屏放大，内容本身不设最大宽度（表格和图表应当用满给它的宽度）。唯一的例外是告警流，在 768px 收为单列。

### 代码组织

- 新增一层**无业务含义的展示组件**目录，与现有的领域组件目录分离。领域组件目录的语义是"带业务含义的共享件"，把通用表格、面板、指标条塞进去会破坏这个语义。
- 这需要修改前端的项目约定文件（当前禁止新增通用目录，且领域组件是一份封闭清单）。**该约定变更作为独立提交单独评审**，先于任何组件落地。

### 合规与部署

- Carbon 依赖链在依赖安装阶段包含遥测上报（运行时不上报）。在构建入口统一设置其关闭开关，使这件事可审计。
- 运行时不得请求任何外部域名；字体与图标全部随二进制嵌入。

### 执行方式

- 分层推进，在一条长期存在的集成分支上汇合，中间态不进主干（视图层替换期间必然出现部分页面可用、部分不可用的状态）。
- 顺序：先提交视觉规范与验收脚本本身 → 变更项目约定 → 在旧实现仍然可用时先稳定测试定位方式 → 建立令牌层、字体、展示组件与导航壳 → 按页面分组并行改造。
- 页面改造并行度上限 3。各实现者共享展示组件层，超出这个数量后对共享层的隐性修改冲突会抵消并行收益。每个实现者的契约：只使用既有的基线组件与令牌，不新增依赖，不修改展示组件层与 API 层，不跨组修改文件，同步维护自己那组的测试。

## Testing Decisions

### 什么是一个好的测试

本次改动的性质是"视图层整体替换、行为不变"。因此好的测试**只断言外部可观察的行为**：页面呈现了什么信息、操作之后发生了什么、无权限时是否被拦住。任何断言组件库生成的类名、DOM 嵌套结构或内部状态的测试，都是本次要消除的负债——它们会在换库时全部变红，且变红不携带任何信息。

### 缝的选择

复用现有的三条缝，**不新增缝**：

1. **纯函数单测（主要的行为基准）。** 派生与呈现逻辑已被抽出组件之外单独测试，这一层与 UI 库完全无关。改造期间这些测试**一行都不应修改**；它们保持绿色就是"行为没变"的最强证据。若路由合并移动了某段派生逻辑，测试随之移动，但不重写。
2. **端到端测试（本次的主缝）。** 针对真实服务与种子数据运行，是仓库里最高的缝。每条路由的功能由它守住。这是唯一有资格判定"重构是否破坏了功能"的层。
3. **组件渲染测试（只减不增）。** 现有的渲染测试保留并迁移定位方式，但**本次不新增任何渲染测试**。这一层最贵、最容易与标记耦合，不应随视图层替换而增长。

**不为展示组件层建立新的测试缝。** 这些原子没有行为，由端到端测试与人工视觉验收覆盖即可。唯一有真实逻辑的新增物是侧栏折叠（持久化、无障碍状态、存储不可用时的降级），其逻辑应抽为纯模块并在第 1 层测试，而不是通过渲染导航来测。

### 定位方式的迁移

当前代码库中**没有任何稳定的测试标识**，测试大量依赖组件库生成的类名与组件库特有的 DOM 属性（表格行属性、标签页角色、徽标点的类名等）。预计视图层替换会波及约三分之一的测试文件。

因此**第一步工作是在旧实现仍然可用时补齐稳定定位方式**：优先使用无障碍角色与标签，无法表达时使用显式测试标识。这一步完成后所有测试应当保持全绿——**那次全绿是整个重构的基线**。此后任何一次变红都必须被解释为真实的行为变更，而不是"样式改了"。

先改实现、最后统一修测试的做法被明确排除：它会把"哪些红是预期的"这一判断累积到最后一刻，而这是一次波及全部路由的改动，功能真的坏了必须当场知道。

### 门禁

项目既有的检查（类型检查、静态检查、单元测试）为硬门禁，每一组页面改造完成即须通过。

### 视觉验收（不是测试缝）

仓库中已有一套可用的问答流水，能启动服务、注入种子数据、登录并对多个视图截图。将其**扩展**到合并后的全部路由，取 1600px 与 1280px 两档宽度，移动档只保留告警流。截图用于人工比对，**不纳入自动断言**——在视觉大幅变动期间维护截图基线只会天天变红。视觉基线的固化留待样式稳定之后。

由于本机资源受限，所有依赖安装、构建、测试与截图均在远程执行环境中运行。

### 现有测试的参照

新增或迁移测试时，参照现有的同类实践：纯函数测试直接对导出的派生函数断言；端到端测试通过角色与可见文本定位，并对真实服务的响应做断言；渲染测试使用测试库配合模拟的查询客户端。

## Out of Scope

- **深色主题的实现。** 只保证令牌结构支持将来一次性覆盖；具体色值的选定是一次独立的设计决策。
- **产品功能的增删。** 不新增功能点，不删除功能点。路由可以合并，能力不能减少。
- **国际化。** 界面文案保持简体中文硬编码，不引入 i18n 框架。
- **后端与 API 变更。** 唯一的例外是错误映射模块的形状调整，这是表单库更换的必然连带。
- **移动端适配。** 除告警流之外，所有界面继续假设使用者坐在桌前。
- **视觉回归基线的自动化。** 截图仅供人工比对。
- **对静态 HTML 原型的像素级复刻。** 采用官方组件库后，原型降级为意图参考；其手写样式表不再是实现目标，其截图不再是验收标准。
- **性能优化。** 除非实测体积显著超出预期，否则不在本次做打包体积的专项优化。

## Further Notes

### 与静态原型的关系

仓库外存在一套静态 HTML 视觉原型，与本仓库的视觉规范文件内容完全一致（逐字节相同）。原型是手写 CSS 实现，与官方组件库的产出并不相同。选择官方组件库意味着接受"视觉基准从原型转移到组件库默认形态 + 令牌覆盖"。如果需求实际上是"最终界面必须与原型截图一致"，则整个技术选型需要推翻——这两者不可兼得。

### 已知的成本，尚未实测

- 打包体积：所需组件子集的估计约为 130–170KB gzip 的 JS 加 35–55KB gzip 的 CSS，另加图表库的 D3 底座与约 148KB 字体。这些数字来自公开的包体积数据与推算，**只有真实构建能确认**。若实测显著超出，应当停下来重新评估，而不是继续改完。
- 开发体验：Sass 编译会明显拖慢热更新。
- 官方文档中关于本项目所用构建工具的配置说明已经过期，实现时不可照抄。

### 依赖安装

前端依赖当前未安装。所有安装与构建都需在远程执行环境完成；若该环境不可达，本地没有替代路径。

### 前置提交

视觉规范文件与问答验收脚本目前均未纳入版本控制。它们分别是本规格的引用源与验收手段，应作为第一个提交入库；本机的 agent 配置目录则应被忽略。
